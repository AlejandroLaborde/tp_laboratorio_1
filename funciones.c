
    float sumar(float operador01, float operador02); //se encarga de la suma
    float restar(float operador01, float operador02); // se encarga de la resta
    float multiplicar(float operador01, float operador02); // se encarga de multiplicar
    float dividir(float operador01, float operador02); // se encarga de la division
    float factorial(float operador);
    void muestraMensaje(char mensaje[], float resultado);
    int esEntero(float valorA);
